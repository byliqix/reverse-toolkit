[package]
name = "xptool-fltk"
version = "1.0.0"
edition = "2021"

[dependencies]
fltk = { version = "1", features = ["fltk-bundled"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
